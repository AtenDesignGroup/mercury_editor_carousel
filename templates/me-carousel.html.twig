{#
/**
 * @file
 * Template for a carousel.
 *
 * Available variables:
 * - attributes: the container attributes
 * - content.carousel_items: the carousel items
 * - region_attributes.carousel_items: the carousel items region attributes
 *
 * @todo Creat a working template. Currently we are overriding and passing
 * variables to the SDC component. Eventually this module should include a
 * default library implementation for a working carousel.
 */
#}

